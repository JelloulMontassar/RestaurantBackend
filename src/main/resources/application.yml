server:
  port: 8080
jwt:
  secret : EFU9oCDT66HO8FoJJmcsvNti4rDrMIwILEaLoOlWomM=
base:
  url: http://localhost:8080
springdoc:
  api-docs:
    path: /v3/api-docs
    url: /swagger-ui.html

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/Restaurant?useSSL=false&serverTimezone=UTC
    username: root
    password: root_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
